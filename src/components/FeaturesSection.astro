<section id="features" class="py-16 px-6 max-w-4xl mx-auto">
  <h2 class="text-2xl font-bold text-white mb-3 text-center tracking-tight">
    Engineered for absolute performance
  </h2>
  <p class="text-sm text-zinc-400 text-center mb-12 max-w-lg mx-auto">
    Everything you need to keep your disk clean across JavaScript, Python, Rust,
    Go, and more.
  </p>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
    <div class="border border-zinc-800 rounded-xl p-6 bg-zinc-900/40">
      <div
        class="text-white font-semibold text-sm mb-2 flex items-center gap-2"
      >
        <span>⚡</span> Parallel Size Measurement
      </div>
      <p class="text-xs text-zinc-400 leading-relaxed">
        Powered by Rayon to scan and measure huge directory trees concurrently
        without blocking your terminal.
      </p>
    </div>
    <div class="border border-zinc-800 rounded-xl p-6 bg-zinc-900/40">
      <div
        class="text-white font-semibold text-sm mb-2 flex items-center gap-2"
      >
        <span>🎯</span> Smart Pruning
      </div>
      <p class="text-xs text-zinc-400 leading-relaxed">
        Never descends into detected target folders, ensuring massive <code
          class="font-mono text-zinc-200">node_modules</code
        > don't slow down the scan.
      </p>
    </div>
    <div class="border border-zinc-800 rounded-xl p-6 bg-zinc-900/40">
      <div
        class="text-white font-semibold text-sm mb-2 flex items-center gap-2"
      >
        <span>🛡️</span> Safe by Default
      </div>
      <p class="text-xs text-zinc-400 leading-relaxed">
        Ambiguous folders like <code class="font-mono text-zinc-200">dist</code
        >, <code class="font-mono text-zinc-200">build</code>, and <code
          class="font-mono text-zinc-200">bin</code
        > are opt-in only via <code class="font-mono text-zinc-200"
          >--include-generic</code
        > to protect production files.
      </p>
    </div>
    <div class="border border-zinc-800 rounded-xl p-6 bg-zinc-900/40">
      <div
        class="text-white font-semibold text-sm mb-2 flex items-center gap-2"
      >
        <span>🎨</span> Themes & Filters
      </div>
      <p class="text-xs text-zinc-400 leading-relaxed">
        Choose from built-in themes (Dracula, Ayu, Gruvbox, Mono) and filter
        directories untouched for months with <code
          class="font-mono text-zinc-200">--older-than</code
        >.
      </p>
    </div>
  </div>
</section>
